#version 400

in vec2 texcoord;
uniform sampler2D tex;

in vec4 n;

out vec4 outcol;

void main(void)
{
	vec4 l = vec4(5.1, -1.1, -20.0, 0.0);
	outcol = (0.1 + vec4(1.0, 1.0, 1.0, 0.0) * clamp(dot(n, l), 0.0, 1.0)) * texture(tex, texcoord);
}
